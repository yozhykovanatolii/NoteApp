<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.NoteApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Button.Color" parent="ThemeOverlay.Material3.FloatingActionButton.Primary">
        <item name="colorContainer">@color/white</item>
        <item name="colorOnContainer">@color/dark_yellow</item>
    </style>
    <style name="ActionButton.Color" parent="ThemeOverlay.Material3.FloatingActionButton.Primary">
        <item name="colorContainer">@color/dark_yellow</item>
        <item name="colorOnContainer">@color/white</item>
    </style>

    <style name="Theme.NoteApp" parent="Base.Theme.NoteApp" />
</resources>